你是一个资深前端架构师，熟悉 Cloudflare Workers、Durable Objects、WebSocket。

请帮我设计一个《我画你猜》网页小游戏的完整技术方案，要求：

无需注册账号登录，一个房间只能两个人同时在线玩，实时画板同步画面，实时聊天功能，画图权限可切换，一个人画另一个人猜，前端页面部署在 Cloudflare Pages，后端仅使用 Cloudflare Workers + Durable Objects；

请给出架构设计、模块拆分和关键数据结构。

请使用React+Twind CSS主技术方案实现一个可实时同步的画板模块：

监听鼠标事件绘制、将绘画轨迹序列化为 JSON通过 WebSocket 发送、能根据收到的轨迹数据在 Canvas 中复现。

请用 TypeScript 编写一个 Cloudflare Durable Object，功能包括：

创建房间、限制最多 2 个 WebSocket 连接、管理房主与画画权限、广播绘画与聊天消息、支持权限切换。

1.进入页面后，有一个创建房间按钮和加入房间按钮；

- 点击创建房间后，会生成一个6位数房间号，在房间页面显示正在等其他玩家进入；

- 默认只能两个人玩，一旦有玩家进入该房间，则房间大门关闭，其他人进不去；

  2.默认房主可以在白板上用鼠标画图案，另一个玩家能实时看到绘画轨迹，等房主画完并输入正确答案，点击确认提交后，另一玩家就可以输入答案猜测了；

- 房主可以把画画权限转给另一个玩家，只能有一个人能绘画，另一个猜；

- 在右侧增加一个实时聊天面板；

  3.功能完成后需要提交到我的代码仓库：https://github.com/bello96/draw-guess；

  4.最后通过github提交的代码实时部署到cloudflare上，域名：https://draw-guess.dengjiabei.cn/；

以上方案是否可靠能实现吗，是否需要维护两套代码，前端和后端，数据库需要我处理吗，Cloudflare 免费方案能否实现？
